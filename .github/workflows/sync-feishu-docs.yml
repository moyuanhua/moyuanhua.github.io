name: 同步飞书文档

on:
  # 每天北京时间 9:00 (UTC 1:00) 自动运行
  schedule:
    - cron: '0 1 * * *'

  # 支持手动触发
  workflow_dispatch:

# 设置权限允许推送到仓库和部署到 Pages
permissions:
  contents: write
  pages: write
  id-token: write

# 防止多个工作流同时运行造成冲突
concurrency:
  group: sync-and-deploy
  cancel-in-progress: false

jobs:
  sync-docs:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 缓存 npm 依赖
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('app/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: 安装依赖
        working-directory: ./app
        run: npm install

      - name: 创建 .env 文件
        working-directory: ./app
        run: |
          cat > .env << EOF
          FEISHU_APP_ID=${{ secrets.FEISHU_APP_ID }}
          FEISHU_APP_SECRET=${{ secrets.FEISHU_APP_SECRET }}
          FEISHU_SPACE_ID=${{ secrets.FEISHU_SPACE_ID }}
          FEISHU_DOCS_NODE_ID=${{ secrets.FEISHU_DOCS_NODE_ID }}
          FEISHU_ABOUT_DOC_ID=${{ secrets.FEISHU_ABOUT_DOC_ID }}
          FEISHU_INCREMENTAL_DAYS=2
          SKIP_FEISHU_SYNC=false
          SITE_URL=https://moyuanhua.github.io
          EOF

      - name: 同步飞书文档
        working-directory: ./app
        run: npm run sync

      - name: 检查是否有变更
        id: check_changes
        run: |
          if [[ -n $(git status -s app/docs app/src/pages/about.md) ]]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "📝 检测到文档变更"
          else
            echo "has_changes=false" >> $GITHUB_OUTPUT
            echo "✅ 文档无变更"
          fi

      - name: 提交变更
        if: steps.check_changes.outputs.has_changes == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # 先添加并提交变更
          git add app/docs app/src/pages/about.md
          git commit -m "docs: 自动同步飞书文档 $(date '+%Y-%m-%d %H:%M:%S')"

          # 推送,如果失败则重试
          for i in {1..3}; do
            if git push origin main; then
              echo "✅ 推送成功"
              break
            else
              echo "⚠️ 推送失败,尝试重新拉取 (尝试 $i/3)"
              # 拉取最新代码并自动解决冲突
              git fetch origin main
              git rebase origin/main || {
                echo "⚠️ 检测到冲突,尝试使用 ours 策略自动解决"
                git rebase --abort
                git pull --rebase -X ours origin main
              }

              if [ $i -eq 3 ]; then
                echo "❌ 推送失败,已达最大重试次数"
                exit 1
              fi
              sleep 2
            fi
          done

      - name: 构建站点
        if: steps.check_changes.outputs.has_changes == 'true'
        working-directory: ./app
        run: npm run build
        env:
          SKIP_FEISHU_SYNC: true
          NODE_ENV: production

      - name: 设置 Pages
        if: steps.check_changes.outputs.has_changes == 'true'
        uses: actions/configure-pages@v4

      - name: 上传构建产物
        if: steps.check_changes.outputs.has_changes == 'true'
        uses: actions/upload-pages-artifact@v3
        with:
          path: app/build

      - name: 部署到 GitHub Pages
        if: steps.check_changes.outputs.has_changes == 'true'
        id: deployment
        uses: actions/deploy-pages@v4

      - name: 完成
        run: |
          if [[ "${{ steps.check_changes.outputs.has_changes }}" == "true" ]]; then
            echo "✅ 文档已同步、提交并部署"
            echo "🌐 部署地址: ${{ steps.deployment.outputs.page_url }}"
          else
            echo "ℹ️ 无需更新"
          fi
